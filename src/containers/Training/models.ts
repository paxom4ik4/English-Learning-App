export interface InitialStateTrainCardLoader {
  isCardLoad: boolean;
}

export interface InitialStateError {
  error: boolean;
}

export interface InitialStateTrainingPage {
  trainingOpen: boolean;
}
