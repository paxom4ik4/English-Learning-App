export interface CardProps {
  route: string;
  cardId: string;
  name: string;
  imgSrc: string;
  modalId: string;
  desc: string;
}

export interface ModalProps {
  modalId: string;
  name: string;
  desc: string;
}
